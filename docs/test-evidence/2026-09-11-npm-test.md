# Evidência de execução — `npm run test`

- **Data**: 2026-09-11
- **Commit**: `8408222`
- **Node.js**: v24.13.1
- **Comando**: `npm run test` (`node --import tsx --test src/utils/insights.test.ts src/utils/csvXlsxParser.test.ts src/utils/pin.test.ts`)
- **Resultado**: 20/20 testes passando, 0 falhas

```
> sistema-gestao-financeira-pessoal@0.2.0 test
> node --import tsx --test src/utils/insights.test.ts src/utils/csvXlsxParser.test.ts src/utils/pin.test.ts

✔ detectStatementFormat recognizes the Nubank PT-BR export (0.6082ms)
✔ detectStatementFormat recognizes the Nubank EN export (0.1015ms)
✔ detectStatementFormat falls back to generic for anything else (0.0814ms)
✔ parseStatementDate handles ISO and DD/MM/YYYY (0.1974ms)
✔ parseStatementAmount handles Brazilian thousand/decimal separators and negatives (0.5994ms)
✔ mapRowsToParsedTransactions maps a Nubank PT-BR row and auto-matches the category by keyword (0.3523ms)
✔ mapRowsToParsedTransactions skips zero-value rows (0.1346ms)
✔ markPossibleDuplicates flags rows matching an existing transaction by date+amount+description (0.4074ms)
✔ groupTransactionsByMonth sums income/expense/investment per month and computes balance (4.8273ms)
✔ getCategoryTrend returns null percentChange when there is no prior month data at all (0.4574ms)
✔ getCategoryTrend compares current month to the average of prior months with data (0.2613ms)
✔ projectMonthEndTotal extrapolates the daily average across the whole month (0.1883ms)
✔ projectMonthEndTotal with a day-1 currentDate: daysElapsed is legitimately 1 (0.1683ms)
✔ getExpenseTrend only averages months chronologically before the queried month, not ones after (0.419ms)
✔ getExpenseTrend returns null percentChange when there is no prior month data at all (0.1914ms)
✔ getSavingsRateSeries adds baseSalary to each month and computes the saved fraction (0.2499ms)
✔ getCumulativeBalanceSeries accumulates month balances in chronological order (0.2772ms)
✔ hashPin produces a deterministic 64-char hex digest (5.6286ms)
✔ hashPin produces different digests for different PINs (1.3778ms)
✔ verifyPin returns true for the matching PIN and false otherwise (1.4805ms)
ℹ tests 20
ℹ suites 0
ℹ pass 20
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 349.0946
```

## Cobertura por área

| Módulo testado | O que é coberto |
|---|---|
| `csvXlsxParser.ts` | Detecção de formato de extrato (Nubank PT-BR/EN/genérico), parsing de data (ISO e DD/MM/YYYY), parsing de valor monetário brasileiro (milhar `.`, decimal `,`, negativos), mapeamento de linha para transação com auto-categorização por palavra-chave, descarte de linhas de valor zero, detecção de duplicata por data+valor+descrição |
| `insights.ts` | Agrupamento mensal de receita/despesa/investimento e saldo, tendência de categoria com e sem dados de meses anteriores, projeção de fechamento de mês (incluindo o caso de borda do dia 1), tendência de gasto respeitando ordem cronológica (regressão do bug de comparar com meses futuros), taxa de poupança, saldo acumulado |
| `pin.ts` | Hash determinístico de PIN, hashes diferentes para PINs diferentes, verificação correta/incorreta |

Para reproduzir: `npm install && npm run test` a partir da raiz do repositório.
